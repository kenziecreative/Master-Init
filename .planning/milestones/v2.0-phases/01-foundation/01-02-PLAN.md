---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - skills/knzinit/SKILL.md
  - .claude-plugin/plugin.json
autonomous: true
requirements: [ARCH-01, ARCH-02, INFR-01, INFR-02]

must_haves:
  truths:
    - "SKILL.md orchestrator describes the two-system architecture, not the 5-layer model"
    - "SKILL.md uses resolve-root.sh for path resolution instead of bare CLAUDE_PLUGIN_ROOT"
    - "SKILL.md instructs scaffolding of .claude/rules/ files from new templates"
    - "SKILL.md no longer references or creates LEARNINGS.md"
    - "SKILL.md instructs adding version markers to all generated files"
    - "plugin.json version is updated to 2.0.0"
  artifacts:
    - path: "skills/knzinit/SKILL.md"
      provides: "Updated orchestrator using two-system architecture and resolve-root.sh"
      contains: "instruction system"
    - path: ".claude-plugin/plugin.json"
      provides: "Plugin manifest at v2.0.0"
      contains: "2.0.0"
  key_links:
    - from: "skills/knzinit/SKILL.md"
      to: "scaffold/resolve-root.sh"
      via: "sources resolve-root.sh for KNZINIT_ROOT"
      pattern: "resolve-root"
    - from: "skills/knzinit/SKILL.md"
      to: "scaffold/templates/CLAUDE.md.tmpl"
      via: "references template for CLAUDE.md generation"
      pattern: "CLAUDE\\.md\\.tmpl"
    - from: "skills/knzinit/SKILL.md"
      to: "scaffold/templates/rules/"
      via: "references rules templates for .claude/rules/ generation"
      pattern: "rules/"
---

<objective>
Rewrite the SKILL.md orchestrator to use the two-system architecture, resolve-root.sh for path resolution, new rules templates, and remove LEARNINGS.md from the scaffolding flow. Update plugin.json to v2.0.0.

Purpose: The orchestrator is what Claude actually executes when a user runs /knzinit. It must reference the new templates and architecture from Plan 01.
Output: Updated SKILL.md orchestrator, plugin.json at v2.0.0.
</objective>

<execution_context>
@/Users/kelseyruger/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kelseyruger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-01-SUMMARY.md

<interfaces>
<!-- From Plan 01: new/updated files that this plan depends on -->

scaffold/resolve-root.sh provides:
- Sourceable script: `. ${CLAUDE_PLUGIN_ROOT}/scaffold/resolve-root.sh` or path-relative sourcing
- Exports: KNZINIT_ROOT (resolved plugin directory), KNZINIT_VERSION (from plugin.json)
- Function: knzinit_version_marker() returns "<!-- knzinit vX.Y.Z -->"

scaffold/templates/CLAUDE.md.tmpl:
- Rewritten for two-system architecture
- Uses {{PROJECT_NAME}}, {{PROJECT_DESCRIPTION}}, {{VERSION}} placeholders
- Under 200 lines, critical instructions in first third

scaffold/templates/rules/session-protocol.md.tmpl:
- Session start/end/compaction protocol
- Uses {{VERSION}} placeholder

scaffold/templates/rules/conventions.md.tmpl:
- Code project conventions placeholder
- Uses {{VERSION}} placeholder

scaffold/templates/STATE.md.tmpl:
- Updated, no LEARNINGS references, has {{VERSION}} marker

REMOVED: scaffold/templates/LEARNINGS.md.tmpl (Plan 01 does NOT delete it, but this plan's SKILL.md must stop referencing it)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite SKILL.md orchestrator for two-system architecture</name>
  <files>skills/knzinit/SKILL.md</files>
  <action>
Rewrite skills/knzinit/SKILL.md. Preserve the overall flow (Step 1 orient, Step 2 explore, Step 3 build, Step 4 merge settings, Step 5 git commit, Step 6 report) but update content throughout. Key changes:

**Frontmatter:**
- Update description: replace "5-layer memory system" with "two-system architecture (instruction + learning)"

**Step 1 (Orientation):** Keep the three questions. No changes needed.

**Step 2 (Explore):**
- Remove check for LEARNINGS.md
- Add check for `.claude/rules/` directory
- Keep all other existing checks

**Step 3A — replace "Five-Layer Memory System" with "Two-System Architecture":**

Path resolution: Instead of bare `${CLAUDE_PLUGIN_ROOT}/scaffold/templates/`, instruct Claude to:
1. First try `${CLAUDE_PLUGIN_ROOT}/scaffold/templates/` if CLAUDE_PLUGIN_ROOT is set
2. If not available, look for the plugin by walking up from the skill file location or checking known plugin directories
3. Reference resolve-root.sh as the canonical resolution logic (SKILL.md is markdown read by Claude, not executed as a script, so the instruction is "use the same logic as resolve-root.sh" or "source resolve-root.sh if running shell commands for path resolution")

Replace the five layers with two systems:

**Instruction System** (static, human-curated):
- **CLAUDE.md** — Create from `CLAUDE.md.tmpl`. Same extend-not-replace logic as before. Template already handles two-system content.
- **.claude/rules/** — NEW: Generate 1-2 starter rules files based on project type:
  - Code projects: copy `rules/session-protocol.md.tmpl` and `rules/conventions.md.tmpl`
  - Non-code projects: copy `rules/session-protocol.md.tmpl` only (conventions not relevant)
  - Substitute {{VERSION}} placeholder with the version from plugin.json

**Learning System** (dynamic, session-updated):
- **STATE.md** — Create from `STATE.md.tmpl`. Same logic as before.
- **Auto Memory (MEMORY.md)** — Same as before: write initial MEMORY.md in auto-memory directory.

Remove entirely:
- Layer 4 (LEARNINGS.md) — do NOT create LEARNINGS.md. Its content merges into auto memory entries. If a LEARNINGS.md already exists in the target project, leave it but do not reference it in the new CLAUDE.md.
- Layer 5 naming — hooks/guardrails remain but are not called "Layer 5". They stay in Step 3B.

**Step 3B (Security & Sanity):** Keep as-is. No changes for Phase 1.

**Step 3C (.gitignore):** Keep as-is.

**Step 3D (Create Directories):**
- Add `.claude/rules/` to the directory list

**Step 4 (Merge Settings):** Keep as-is.

**Step 5 (Git Commit):** Keep as-is.

**Step 6 (Report):**
- Update the summary to reference two-system architecture
- Add note: after scaffolding, show a brief 3-4 line summary of the two-system architecture (per CONTEXT.md decision)
- Remove references to LEARNINGS.md from the report

**Version markers:**
- Add instruction: "After generating each file from a template, ensure the version marker comment `<!-- knzinit vX.Y.Z -->` is present at the bottom. Read the version from `.claude-plugin/plugin.json`."

Keep the same overall tone and structure. The SKILL.md should remain readable and actionable for Claude — it IS the prompt.
  </action>
  <verify>
    <automated>bash -c 'cd /Users/kelseyruger/Projects/a-emporium-working/gold-master-projects/init-agent && grep -q "instruction system" skills/knzinit/SKILL.md && grep -q "learning system" skills/knzinit/SKILL.md && grep -q "resolve-root" skills/knzinit/SKILL.md && grep -q "rules/" skills/knzinit/SKILL.md && grep -q "version" skills/knzinit/SKILL.md && ! grep -qi "five-layer\|5-layer\|Layer 4.*LEARNINGS\|Layer 5" skills/knzinit/SKILL.md && echo "PASS"'</automated>
  </verify>
  <done>SKILL.md uses two-system terminology, references resolve-root.sh for path resolution, generates .claude/rules/ files, does not reference LEARNINGS.md creation or 5-layer model, and includes version marker instructions.</done>
</task>

<task type="auto">
  <name>Task 2: Update plugin.json to v2.0.0 and delete LEARNINGS.md.tmpl</name>
  <files>.claude-plugin/plugin.json, scaffold/templates/LEARNINGS.md.tmpl</files>
  <action>
1. Update .claude-plugin/plugin.json:
   - Change "version" from "1.0.0" to "2.0.0"
   - Update "description" from "Bootstrap projects with a 5-layer memory system, security agents, guardrail hooks, and sanity checks" to "Bootstrap projects with two-system architecture (instruction + learning), security agents, guardrail hooks, and sanity checks"

2. Delete scaffold/templates/LEARNINGS.md.tmpl — this template is no longer part of the v2 scaffold. LEARNINGS.md content merges into auto memory entries per user decision.

Both changes are small but important: plugin.json version is the single source of truth for all version markers (INFR-02), and removing LEARNINGS.md.tmpl prevents accidental use.
  </action>
  <verify>
    <automated>bash -c 'cd /Users/kelseyruger/Projects/a-emporium-working/gold-master-projects/init-agent && grep -q "2.0.0" .claude-plugin/plugin.json && grep -q "two-system" .claude-plugin/plugin.json && test ! -f scaffold/templates/LEARNINGS.md.tmpl && echo "PASS"'</automated>
  </verify>
  <done>plugin.json is at v2.0.0 with updated description. LEARNINGS.md.tmpl is deleted.</done>
</task>

</tasks>

<verification>
The /knzinit skill, when read by Claude, will instruct scaffolding using two-system architecture terminology, generate .claude/rules/ files, skip LEARNINGS.md, use fallback-safe path resolution, and stamp all generated files with version markers from plugin.json v2.0.0.
</verification>

<success_criteria>
1. SKILL.md contains "instruction system" and "learning system", not "5-layer" or "five-layer"
2. SKILL.md references resolve-root.sh or its logic for path resolution
3. SKILL.md generates .claude/rules/ files from templates
4. SKILL.md does not create LEARNINGS.md
5. SKILL.md includes version marker instructions
6. plugin.json version is "2.0.0"
7. LEARNINGS.md.tmpl no longer exists
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
