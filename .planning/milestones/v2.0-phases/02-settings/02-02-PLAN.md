---
phase: 02-settings
plan: 02
type: execute
wave: 1
depends_on: ["02-01"]
files_modified: ["README.md"]
autonomous: true
gap_closure: true
requirements: ["SETT-01", "SETT-02", "SETT-03", "SETT-04", "SETT-05", "SETT-06"]

must_haves:
  truths:
    - "README.md guardrails line references scaffold/templates/settings.json.tmpl, not deleted scaffold/settings.json"
  artifacts:
    - path: "README.md"
      provides: "Correct path reference for settings template"
      contains: "scaffold/templates/settings.json.tmpl"
  key_links:
    - from: "README.md"
      to: "scaffold/templates/settings.json.tmpl"
      via: "documentation reference"
      pattern: "scaffold/templates/settings\\.json\\.tmpl"
---

<objective>
Fix stale path reference in README.md that still points to deleted scaffold/settings.json.

Purpose: Verification found README.md line 136 directs users to a non-existent file. The correct path is scaffold/templates/settings.json.tmpl.
Output: Updated README.md with correct template path.
</objective>

<execution_context>
@/Users/kelseyruger/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kelseyruger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-settings/02-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix stale settings.json path in README.md</name>
  <files>README.md</files>
  <action>
    Update README.md line 136 to replace the stale path reference.

    Change:
    `- **Change automated guardrails**: Edit files in `scaffold/hooks/` and `scaffold/settings.json``

    To:
    `- **Change automated guardrails**: Edit files in `scaffold/hooks/` and `scaffold/templates/settings.json.tmpl``

    This is a single-line documentation fix. Do not change any other lines.
  </action>
  <verify>
    <automated>grep -n "scaffold/templates/settings.json.tmpl" README.md | grep -q "136" && echo "PASS: Line 136 references correct path" || echo "FAIL: Line 136 does not reference correct path"</automated>
  </verify>
  <done>README.md line 136 references scaffold/templates/settings.json.tmpl instead of deleted scaffold/settings.json. No other lines changed.</done>
</task>

</tasks>

<verification>
- `grep "scaffold/settings.json" README.md` returns NO matches (stale path fully removed)
- `grep "scaffold/templates/settings.json.tmpl" README.md` returns the guardrails line
</verification>

<success_criteria>
README.md contains zero references to the deleted scaffold/settings.json path. The guardrails customization line points to scaffold/templates/settings.json.tmpl.
</success_criteria>

<output>
After completion, create `.planning/phases/02-settings/02-02-SUMMARY.md`
</output>
