---
phase: 06-audit-gap-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - INT-01

must_haves:
  truths:
    - "README.md describes two-system architecture (instruction + learning), not 5-layer model"
    - "README.md directory tree matches actual v2 scaffold file structure"
    - "README.md adaptive behavior table reflects universal session hooks for all project types"
    - "README.md does not reference LEARNINGS.md.tmpl, scaffold/settings.json, or 5-layer terminology"
  artifacts:
    - path: "README.md"
      provides: "Accurate v2 project documentation"
      contains: "two-system architecture"
  key_links:
    - from: "README.md"
      to: "skills/knzinit/SKILL.md"
      via: "Architecture description consistency"
      pattern: "instruction system.*learning system"
---

<objective>
Rewrite README.md to accurately describe the v2 two-system architecture, replacing all 5-layer model references, stale file listings, and outdated adaptive behavior descriptions.

Purpose: README.md is the public face of the plugin — it currently describes a deleted architecture (5-layer model, LEARNINGS.md.tmpl, scaffold/settings.json). This closes INT-01 (high priority).
Output: README.md rewritten with accurate v2 content.
</objective>

<execution_context>
@/Users/kelseyruger/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kelseyruger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v2.0-MILESTONE-AUDIT.md

@README.md
@skills/knzinit/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite README.md for v2 two-system architecture</name>
  <files>README.md</files>
  <action>
Rewrite README.md to accurately reflect the v2 plugin. Keep the same general section structure (intro, plugin structure, what it does, where it fits, skills, what it scaffolds, adaptive behavior, extending) but update all content.

Specific changes required:

1. **Opening paragraph**: Replace "5-layer memory system" with "two-system architecture (instruction + learning), session lifecycle hooks, and adaptive health checks". Remove "security agents" from the lead — they're code-project-only, not universal.

2. **Plugin Structure tree**: Replace the entire tree with actual v2 structure:
```
init-agent/
├── .claude-plugin/
│   └── plugin.json
├── skills/
│   └── knzinit/
│       └── SKILL.md
├── scaffold/
│   ├── agents/
│   │   ├── security-scanner.md
│   │   └── secrets-env-auditor.md
│   ├── hooks/
│   │   ├── hook-utils.sh
│   │   ├── milestone-check.sh
│   │   ├── post-compact-orientation.sh
│   │   ├── pre-commit-secrets.sh
│   │   ├── pre-compact-check.sh
│   │   ├── session-end.sh
│   │   └── session-start.sh
│   ├── skills/
│   │   ├── handoff/
│   │   │   └── SKILL.md
│   │   ├── resume/
│   │   │   └── SKILL.md
│   │   └── sanity-check/
│   │       └── SKILL.md
│   ├── templates/
│   │   ├── CLAUDE.md.tmpl
│   │   ├── STATE.md.tmpl
│   │   ├── decisions-archive.md.tmpl
│   │   ├── gitignore.tmpl
│   │   ├── mcp.json.tmpl
│   │   ├── settings.json.tmpl
│   │   └── rules/
│   │       ├── conventions.md.tmpl
│   │       └── session-protocol.md.tmpl
│   └── resolve-root.sh
└── README.md
```

3. **What It Scaffolds section**: Replace the "5-Layer Memory System" heading and table with "Two-System Architecture":
   - **Instruction system** (static, human-curated): CLAUDE.md + .claude/rules/ — loaded automatically, tells Claude how to behave
   - **Learning system** (dynamic, session-updated): STATE.md + auto memory (MEMORY.md) — updated each session, tracks project state and learnings
   Describe each generated file briefly under these two headings. Do NOT include LEARNINGS.md (deleted in v2).

4. **Hooks section**: List ALL v2 hooks:
   - hook-utils.sh (shared library, sourced by all hooks)
   - pre-compact-check.sh (PreCompact)
   - post-compact-orientation.sh (PostCompact)
   - session-start.sh (SessionStart — 4 matchers: startup, resume, clear, compact)
   - session-end.sh (SessionEnd)
   - milestone-check.sh (Stop — git only)
   - pre-commit-secrets.sh (PreToolUse — git only)

5. **Skills section**: Add /handoff and /resume alongside /sanity-check. Brief descriptions:
   - /sanity-check: Adaptive project health check
   - /handoff: Captures session state to STATE.md for continuity
   - /resume: Reads STATE.md and outputs orientation summary

6. **Adaptive Behavior table**: Replace with v2 reality. Universal hooks for ALL projects (session-start, session-end, pre-compact, post-compact). Git-only extras (milestone-check, pre-commit-secrets). Non-code projects get different sanity checks and skip security agents.

Updated table columns: Project Type | Instruction System | Learning System | Hooks | Security | Skills

| **Code + Git** | CLAUDE.md + rules (code variant) | STATE.md + auto memory | All hooks | Agents + pre-commit secrets | All skills |
| **Code, no Git** | CLAUDE.md + rules (code variant) | STATE.md + auto memory | Session hooks only | Agents only | All skills |
| **Non-code + Git** | CLAUDE.md + rules (non-code variant) | STATE.md + auto memory | Session + git hooks | Skipped | All skills (non-code sanity check) |
| **Non-code, no Git** | CLAUDE.md + rules (non-code variant) | STATE.md + auto memory | Session hooks only | Skipped | All skills (non-code sanity check) |

7. **Remove all stale references**: No mentions of LEARNINGS.md, LEARNINGS.md.tmpl, scaffold/settings.json (file was moved to scaffold/templates/settings.json.tmpl), 5-layer, "Layer 1/2/3/4/5", or the 5-layer table.

8. **Extending section**: Update the bullet for settings to reference `scaffold/templates/settings.json.tmpl` (not `scaffold/settings.json`). Add bullets for hooks and skills directories.
  </action>
  <verify>
    <automated>cd /Users/kelseyruger/Projects/a-emporium-working/gold-master-projects/init-agent && grep -c "5-layer\|5 layer\|Layer 1\|Layer 2\|Layer 3\|Layer 4\|Layer 5\|LEARNINGS.md\|scaffold/settings.json[^.]" README.md | grep -q "^0$" && grep -q "two-system architecture" README.md && grep -q "instruction system" README.md && grep -q "learning system" README.md && grep -q "hook-utils.sh" README.md && grep -q "handoff" README.md && grep -q "resume" README.md && grep -q "settings.json.tmpl" README.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>README.md accurately describes v2 two-system architecture. Zero references to 5-layer model, LEARNINGS.md, or scaffold/settings.json. Directory tree matches actual file structure. Adaptive behavior table reflects universal session hooks.</done>
</task>

</tasks>

<verification>
- `grep -c "5-layer\|LEARNINGS" README.md` returns 0
- `grep -c "two-system" README.md` returns >= 1
- Directory tree in README matches output of `find scaffold/ -type f | sort`
- Adaptive behavior table includes rows for non-code projects with session hooks
</verification>

<success_criteria>
README.md accurately describes v2 architecture with no stale references. INT-01 closed.
</success_criteria>

<output>
After completion, create `.planning/phases/06-audit-gap-closure/06-01-SUMMARY.md`
</output>
