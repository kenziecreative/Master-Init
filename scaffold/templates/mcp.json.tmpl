{
  "mcpServers": {
    // Add your project-level MCP servers here.
    // Each entry follows this pattern:
    //
    // "server-name": {
    //   "command": "npx",
    //   "args": ["-y", "@scope/mcp-package-name"],
    //   "env": {
    //     "API_KEY": "${YOUR_API_KEY_ENV_VAR}"
    //   }
    // }
    //
    // Reference: https://docs.anthropic.com/en/docs/claude-code/mcp
    // This file configures project-level MCP servers (applies to this project only).
    // User-level MCP servers belong in ~/.claude/mcp.json instead.
  }
}
// knzinit v{{VERSION}}
